{
  "name": "@mp-publishing/draft-connector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "lint": "tsc --project tsconfig.json --noEmit",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "format": "echo draft-connector has no formatter configured yet"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.3"
  }
}
